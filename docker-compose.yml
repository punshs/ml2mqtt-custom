version: '3.8'

services:
  ml2mqtt:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - ./data:/data
    environment:
      - FLASK_ENV=development
      - FLASK_DEBUG=1
    restart: unless-stopped
